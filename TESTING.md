# MVP 验证记录

## 0.1.2 直接返回

移除编辑页返回确认弹窗。新增回归测试覆盖页面返回按钮、系统返回键和未保存草稿不落库；同时运行全部设备测试、规则测试和 Lint。

结果：13 项设备测试、24 条规则断言、构建及静态检查通过，APK 签名验证通过。

## 0.1.1 日期时间选择器与提醒设置

新增五项设备测试，覆盖月历与时间切换、确认回写、取消不修改原值、闰年 2 月 29 日、跨年切月、未确认状态恢复，以及时间滚轮实际滚动。继续运行已有五项设备集成测试、17 条 JVM 断言、离线配置检查和 Android Lint。

新增独立提醒子页面及自定义提前量，旧数据库无损升级，完成事项恢复未完成。相关测试覆盖子页面返回保存、多个预设与自定义组合、取消发生时提醒、旧版迁移、空提醒配置，以及详情恢复未完成。调度和重启恢复重新验证。

本版 24 条规则断言和 12 项设备集成测试全部通过；构建和 Lint 通过（0 错误，保留两条既有提示），最终 APK 签名验证通过。进程退出后的两条未来闹钟保留、重启解锁后的提醒恢复和持久化读取均再次通过。

## 0.1.0 MVP

验证日期：2026-09-13。环境：Windows、JDK 17、Android 15（API 35）专用模拟器。

结果：17 条 JVM 断言、离线配置检查、5 项设备集成测试、重启前后两阶段探针全部通过。应用进程退出后的两条闹钟保留、重启解锁后的两条闹钟恢复均通过。Debug APK 和测试包构建成功，APK 签名验证通过。

Lint 为 0 错误、2 条提示：固定测试运行器版本有可用更新，以及 Android 13 的返回行为属性在低版本被忽略；后者在低版本使用已有的系统返回处理。两项不阻止构建。

## 自动化验证

| 验证项 | 范围 |
| --- | --- |
| JVM 规则测试 | 17 条断言：三种提醒组合、到期边界、过期过滤、完成状态、跨天结束时间、输入校验、修改开始时间后的重算 |
| 离线配置检查 | 不声明 INTERNET 权限，关闭自动备份 |
| Android 构建 | Debug APK、设备测试 APK、Lint 静态检查 |
| 数据库测试 | 保存后重新打开数据库、字段持久化、修改、完成、删除 |
| 旧通知测试 | 旧版本通知不能完成已编辑的事项 |
| 界面流程测试 | 新建、持续时间与提前提醒录入、编辑、完成、已完成列表；保存截图 |
| 状态冲突测试 | 编辑期间从通知完成事项，再保存不能撤销完成状态 |
| 系统闹钟测试 | 真实 AlarmManager 到期后产生通知；通知完成操作取消通知并保存完成状态 |
| 生命周期测试 | 应用进程退出后保留两条未来闹钟；模拟器重启并解锁后恢复两条闹钟；再次读取持久化事项 |

执行命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/device-test.ps1 -Reboot
```

生命周期测试分为重启前写入和重启后读取两阶段，由脚本驱动；直接运行全部设备测试时，该探针测试会跳过。测试仅限专用模拟器，会清理测试数据。

## 真机验收清单

模拟器结果不能代替厂商真机的省电策略验证。以下项目仍需在目标手机完成：

- 首次安装时通知／精确提醒权限的允许、拒绝、撤销和再次开启。
- 锁屏、长时间待机、省电模式、从最近任务划走后，开始提醒与提前提醒的实际到达情况。
- 重启后首次解锁，确认未来提醒恢复；本版在首次解锁后恢复，不支持解锁前提醒。
- 点击通知进入对应事项，通知中完成后不再触发该事项剩余提醒。
- 字体放大、小屏幕、系统返回、编辑时旋转屏幕和键盘遮挡。
- 断网使用完整流程；Android 8–14 的兼容性实机验证。

已知边界：强行停止应用会影响系统调度，需要重新打开应用恢复未来提醒；关机或关闭通知期间不保证提醒，已过期提醒不集中补发。当前为测试签名 APK，未进行应用商店发布或正式签名。
